# Copy this file to `.env.local` and adjust the values for your environment.

# Base URL of the deployed application (used in metadata generation)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Human readable name for the application. This is consumed by metadata helpers
# and is safe to expose to the client.
NEXT_PUBLIC_APP_NAME=Next.js Starter
